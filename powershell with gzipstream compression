#payload -> compress -> base64 | Compression GzipStream
import base64

#Decode
code=""
decoded=base64.b64decode(code)

#Save the compress payload in a gzip, unzip an then use scdbg
f=open("decoded.gzip","wb")
f=write(decoded)
f.close()
